const {sum, subtract, multiply, divide} = require('./math.js') // Imports The Functions From './Math'

console.log(`Calculating Addition: ${sum(2, 2)}`) // Calculates 4
console.log(`Calculating Subtraction: ${subtract(12, 24)}`) // Calculates -12
console.log(`Calculating Multiplication: ${multiply(20, 5)}`) // Calculates 100
console.log(`Calculating Division: ${divide(121, 11)}`) // Calculates 11